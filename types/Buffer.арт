используется артель

внешние
{
  тип Buffer<T> = объект на основе ObservableObject
  {
    постоянное capacity: Число
    постоянное count: Число
    операция put(items: Массив<T>): Ничего // операция put(...items: Массив<T>): Ничего
    операция take(count: Число):Массив< T>
    общая операция create<T>(hint: Текст? = пусто, capacity: Число? = пусто): Buffer<T>
  }
}
